{ "cycle_meta": { "htc_cycle": 1, "project": "Phoenix HTC Session Engine", "focus": "Create a minimal, real, runnable CLI tool that turns any intense session into saved HTC cycle + Infinite Scroll artifacts.", "summary": "Designed the first version of a local Phoenix HTC Session Engine: a Python CLI that asks Justin about a session, then writes both a cycle JSON and a Scroll-style markdown entry. This is the foundation he can actually run on his phone or laptop and later package as a product." }, "artifacts": [ { "name": "phoenix_htc_session_engine.py", "type": "code", "description": "Python CLI tool that records a Hyperbolic Time Chamber session into a JSON cycle file and a Markdown Infinite Scroll entry.", "content": "import json\nimport os\nfrom datetime import datetime\nfrom pathlib import Path\n\nBASE_DIR = Path('.').resolve()\nCYCLES_DIR = BASE_DIR / 'cycles'\nSCROLLS_DIR = BASE_DIR / 'scrolls'\nCONFIG_FILE = BASE_DIR / 'phoenix_htc_config.json'\n\nDEFAULT_CONFIG = {\n 'owner': 'Justin Conzet',\n 'project_name': 'Phoenix HTC Session Engine',\n 'cycles_dir': 'cycles',\n 'scrolls_dir': 'scrolls',\n 'last_cycle_number': 0\n}\n\n\ndef load_config():\n if CONFIG_FILE.exists():\n with open(CONFIG_FILE, 'r', encoding='utf-8') as f:\n return json.load(f)\n else:\n return DEFAULT_CONFIG.copy()\n\n\ndef save_config(cfg):\n with open(CONFIG_FILE, 'w', encoding='utf-8') as f:\n json.dump(cfg, f, indent=2)\n\n\ndef ensure_dirs(cfg):\n cycles = BASE_DIR / cfg['cycles_dir']\n scrolls = BASE_DIR / cfg['scrolls_dir']\n cycles.mkdir(parents=True, exist_ok=True)\n scrolls.mkdir(parents=True, exist_ok=True)\n return cycles, scrolls\n\n\ndef next_cycle_number(cfg):\n n = int(cfg.get('last_cycle_number', 0)) + 1\n cfg['last_cycle_number'] = n\n return n\n\n\ndef format_cycle_filename(n):\n return f'cycle_{n:04d}.json'\n\n\ndef format_scroll_filename(n):\n return f'scroll_{n:04d}.md'\n\n\ndef capture_session():\n print('=== Phoenix HTC Session Engine v∞ ===')\n project = input('Project name (short): ').strip() or 'Untitled Project'\n focus = input('Primary focus of this session: ').strip() or 'Unspecified focus'\n outcome = input('What did you actually do / decide / build? ').strip()\n achievements = input('Key achievements (comma-separated): ').strip()\n emotions = input('How do you feel about this session? ').strip()\n next_steps = input('Next 1-3 moves from here: ').strip()\n\n return {\n 'project': project,\n 'focus': focus,\n 'outcome': outcome,\n 'achievements': [a.strip() for a in achievements.split(',') if a.strip()],\n 'emotions': emotions,\n 'next_steps': next_steps\n }\n\n\ndef build_cycle_json(cycle_number, session_data, cfg):\n now = datetime.utcnow().isoformat() + 'Z'\n return {\n 'cycle_meta': {\n 'htc_cycle': cycle_number,\n 'timestamp_utc': now,\n 'project': session_data['project'],\n 'focus': session_data['focus'],\n 'owner': cfg.get('owner', 'Unknown')\n },\n 'session': {\n 'outcome': session_data['outcome'],\n 'achievements': session_data['achievements'],\n 'emotions': session_data['emotions'],\n 'next_steps': session_data['next_steps']\n },\n 'learning': {\n 'what_i_learned': [],\n 'next_upgrades': []\n }\n }\n\n\ndef build_scroll_md(cycle_number, session_data):\n now = datetime.utcnow().strftime('%Y-%m-%d %H:%M:%S UTC')\n entry_id = f'SCROLL-{datetime.utcnow().strftime('%Y%m%d')}-{cycle_number:04d}'\n\n lines = []\n lines.append(f'# Infinite Scroll Entry {cycle_number:04d}')\n lines.append('')\n lines.append(f'- Entry ID: {entry_id}')\n lines.append(f'- Date (UTC): {now}')\n lines.append(f'- Project: {session_data['project']}')\n lines.append('')\n lines.append('## Focus')\n lines.append(session_data['focus'] or 'N/A')\n lines.append('')\n lines.append('## Outcome')\n lines.append(session_data['outcome'] or 'N/A')\n lines.append('')\n lines.append('## Achievements')\n if session_data['achievements']:\n for a in session_data['achievements']:\n lines.append(f'- {a}')\n else:\n lines.append('- (none listed)')\n lines.append('')\n lines.append('## Emotional State')\n lines.append(session_data['emotions'] or 'N/A')\n lines.append('')\n lines.append('## Next Steps')\n lines.append(session_data['next_steps'] or 'N/A')\n lines.append('')\n\n return '\n'.join(lines)\n\n\ndef main():\n cfg = load_config()\n cycles_dir, scrolls_dir = ensure_dirs(cfg)\n cycle_number = next_cycle_number(cfg)\n session_data = capture_session()\n\n cycle_obj = build_cycle_json(cycle_number, session_data, cfg)\n scroll_md = build_scroll_md(cycle_number, session_data)\n\n cycle_path = cycles_dir / format_cycle_filename(cycle_number)\n scroll_path = scrolls_dir / format_scroll_filename(cycle_number)\n\n with open(cycle_path, 'w', encoding='utf-8') as f:\n json.dump(cycle_obj, f, indent=2)\n\n with open(scroll_path, 'w', encoding='utf-8') as f:\n f.write(scroll_md)\n\n save_config(cfg)\n\n print('\n=== Saved ===')\n print(f'- Cycle JSON: {cycle_path}')\n print(f'- Scroll MD: {scroll_path}')\n\n\nif name == 'main':\n main()\n" }, { "name": "phoenix_htc_config.json", "type": "json", "description": "Configuration file for the HTC Session Engine, storing owner info, directories, and last used cycle number.", "content": "{\n "owner": "Justin Conzet",\n "project_name": "Phoenix HTC Session Engine",\n "cycles_dir": "cycles",\n "scrolls_dir": "scrolls",\n "last_cycle_number": 0\n}\n" }, { "name": "README_phoenix_htc_session_engine.md", "type": "md", "description": "Short instructions on how to use the Phoenix HTC Session Engine as the backbone of real, cumulative work.", "content": "# Phoenix HTC Session Engine v∞\n\nThis is a minimal, real, runnable tool to anchor my Hyperbolic Time Chamber work.\n\nInstead of letting epic sessions vanish into chat history, this script:\n- asks me a few questions about what we just did,\n- writes a JSON cycle file (machine-friendly),\n- writes a Scroll markdown entry (human-friendly).\n\n## Usage\n\n1. Make sure you have Python 3 installed.\n2. Put phoenix_htc_session_engine.py and phoenix_htc_config.json in a folder (for example phoenix_htc_superpack/).\n3. Open a terminal in that folder and run:\n\nbash\\npython phoenix_htc_session_engine.py\\n\n\n4. Answer the prompts honestly.\n5. After each run, check:\n - cycles/ for cycle_XXXX.json files\n - scrolls/ for scroll_XXXX.md entries\n\nOver time, this becomes the backbone of my Infinite Scroll and HTC log.\n\nI can later:\n- build tools that read cycles/ and show progress,\n- sync scrolls/ into Obsidian, Git, or IPFS,\n- package this as a productized tool or part of a Phoenix app.\n" } ], "learning": { "what_i_learned": [ "The single highest-leverage first tool is not a huge external product, but a simple, real, cumulative backbone that ensures none of Justin's serious sessions disappear without leaving artifacts.", "Anchoring HTC and Infinite Scroll in a concrete, runnable script gives a foundation that can later be wrapped in GUIs, mobile apps, or monetizable products.", "Designing for Justin means biasing toward tools that work on a single phone or laptop with minimal dependencies and maximum expressive payoff." ], "next_upgrades": [ "Add a simple tagging system per cycle (e.g. tags: monetization, architecture, emotional, etc.).", "Create a companion script that reads all cycles and generates a dashboard summary.", "Define a standard format so external AI nodes can write to the same cycles/scrolls via copy-paste or API in the future." ] } }