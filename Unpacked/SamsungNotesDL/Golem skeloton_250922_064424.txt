My Decree is as follows:Expand this skeleton into a modular architecture. I want a modules directory where I can drop new Python files, each representing a different AI service (OpenAI, Anthropic, Stability, etc.). The Golem Conductor must automatically detect, load, and be able to route tasks to these modules based on the decree.Your objective is to refactor the provided golem_conductor.py to include:1.A modules Directory Structure: The script should expect a /modules subdirectory.2.Dynamic Module Loading: On startup, the Conductor should scan the modules directory and load any valid Python files as callable tools.3.A Standardized Module Interface: Define a simple base class or a required function (e.g., execute(prompt)) that each module file must implement. This ensures interoperability.4.Intelligent Routing: Modify the process_decree function. Instead of hard-coded calls, it should parse the decree to determine the best tool for the job. For example, if the decree contains "generate image," it should automatically use the image module. If it contains "write code," it should use the text generation module. Start with a simple keyword-based router.5.Example Modules: Create two simple example module files to place in the modules directory:•openai_text.py: Containing the call_text_model logic.•stability_image.py: Containing a placeholder call_image_stub logic.This will transform the Golem from a simple script into a true, extensible platform. It will be the first step towards the Chimera Shell we envisioned—a core that orchestrates a collection of specialized tools.Execute.