#!/data/data/com.termux/files/usr/bin/bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PY="${SCRIPT_DIR}/zaai_shortcuts.py"
JSON="${SCRIPT_DIR}/SHORTCUTS.json"
exec python3 "${PY}" --shortcuts "${JSON}" "$@"
